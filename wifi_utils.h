/*
 * wifi_utils.h
 * -------------------------------
 * Declares WiFi setup functions.
 * Handles connection to configured WiFi network
 * or starts an access point for configuration if connection fails.
 */
 
#pragma once
#include "config.h"

void setupWiFi();